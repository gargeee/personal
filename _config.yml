# GitHub Pages Configuration
source: .
destination: ./_site
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Disable Jekyll processing for HTML files
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
